## Lab610服务器安全配置指南

### 1.禁用root用户登录

首先通过修改ssh配置文件禁用root用户远程登录

```bash
nano /etc/ssh/sshd_config
```

将配置文件中的：

```bash
PermitRootLogin yes
```

改成

```bash
PermitRootLogin no
```

然后重启ssh使改变生效：

```bash
sudo systemctlrestart ssh.service
```

### 2.禁用账号/密码登录，改用密钥登录

首先创建密钥对：

```bash
ssh-keygen -t rsa
```

直接回车所有选项，直到看到生成的RSA密钥

查看密钥对：

```bash
ls ~/.ssh/
```

将公钥上传到服务器：

```bash
ssh-copy-id zzy@[地址]
```

通过修改ssh配置文件禁用账户/密码登录：

```bash
nano /etc/ssh/sshd_config
```

将配置文件中的：

```bash
PasswordAuthentication yes
```

改成：

```bash
PasswordAuthentication no
```

添加：

```bash
RSAAuthentication yes
PubkeyAuthentication yes
```

然后重启ssh使改变生效：

```bash
sudo systemctlrestart ssh.service
```

### 3.更改默认端口

SSH默认端口在$22$

首先通过修改ssh配置文件修改ssh端口：

```bash
nano /etc/ssh/sshd_config
```

将配置文件中的：

```bash
Port 22
```

改成：

```bash
Port 2024
```

然后修改一下防火墙允许链接：

```bash
sudo ufw allow 2024/tcp
```

查看状态：

```bash
sudo ufw status
```

然后重启ssh使改变生效：

```bash
sudo systemctlrestart ssh.service
```

### 