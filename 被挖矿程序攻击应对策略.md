1. 查看当前进程情况

   ```bash
   nvidia-smi
   ```

![img](file:////Users/zhangzeyu/Library/Containers/com.kingsoft.wpsoffice.mac/Data/tmp/wps-zhangzeyu/ksohtml//wps1.jpg)小占寸高功耗进程即为攻击程序

2. 查看权限

   ```bash
   lsattr /usr/bin/nvc
   ```

![img](file:////Users/zhangzeyu/Library/Containers/com.kingsoft.wpsoffice.mac/Data/tmp/wps-zhangzeyu/ksohtml//wps2.jpg) 

3. 切换root

   ```bash
   sudo su root 
   ```

4. 删除权限

   ```bash  
   chatter -R -ia /usr/bin/nvc
   ```

5. 查看权限

   ```bash
   lsattr /usr/bin/nvc 
   ```

6. 删除攻击文档（如果chatter被他损坏）

   ```bash  
   rm -rf /usr/bin/nvc
   ```

把好的chatter拷贝到自己的路径下  /home/sty/chatter -R去删除权限

7. 去查看进程nvidia-smi 

   ```bash
   rm -rf /usr/bin/nvc
   ```

8. 删除进程

   ```bash
   kill -9 进程号 
   ```

   