# 挖矿程序入侵维护记录 2025.1.13

- 使用`nvidia-smi`命令查看当前运行进程

  <img src="/Users/zhangzeyu/Library/Application Support/typora-user-images/image-20250113103941265.png" alt="image-20250113103941265" style="zoom:50%;" />

  可见未知程序` /usr/bin/nvc`占用大量显存，整体GPU功耗偏高

- 记录进程号，查看进程是由哪个用户创建的`ps -o user,pid,ppid,cmd -p [进程号]`

  <img src="/Users/zhangzeyu/Library/Application Support/typora-user-images/image-20250113104448197.png" alt="image-20250113104448197" style="zoom:50%;" />

- 该进程的命令行参数显示它是一个挖矿程序，具体信息如下：

  - 命令行参数：
    - `--proxy 172.18.4.50:1084`：通过代理服务器连接。
    - `--algo kawpow`：使用KawPow算法进行挖矿。
    - `--server 51.89.22.137:10400`：连接到挖矿服务器的IP地址和端口。
    - `--user REvZPwc47hHGDi1ZMjSQ8xxE6Y`：挖矿用户的钱包地址或用户名。

  **为什么这是一个挖矿程序**

  - **算法**：`kawpow` 是一种挖矿算法，通常用于挖取特定的加密货币，如Ravencoin等。
- **服务器**：`51.89.22.137:10400` 是一个挖矿服务器的IP地址和端口，归属地：德国 黑森州 美因河畔法兰克福,挖矿程序会连接到这个服务器进行挖矿操作。
  - **用户**：`REvZPwc47hHGDi1ZMjSQ8xxE6Y` 是一个钱包地址或用户名，用于标识挖矿收益的归属。

- `journalctl -b | grep nvc`查看日志获取进程的详细信息：

  <img src="/Users/zhangzeyu/Library/Application Support/typora-user-images/image-20250113104748884.png" alt="image-20250113104748884" style="zoom:50%;" />

  可见，他并不是定时任务启动的，大概是他登录了root账户，然后用命令行启动的。

- 清理挖矿程序

  1. 查看当前进程情况
  
     ```bash
     nvidia-smi
     ```
  
  小占寸高功耗进程即为攻击程序
  
  2. 查看权限
  
     ```bash
     lsattr /usr/bin/nvc
     ```
  
  
  3. 切换root
  
     ```bash
     sudo su root 
     ```
  
  4. 删除权限
  
     ```bash  
     chatter -R -ia /usr/bin/nvc
     ```
  
  5. 查看权限
  
     ```bash
     lsattr /usr/bin/nvc 
     ```
  
  6. 删除攻击文档（如果chatter被他损坏,把好的chatter拷贝到自己的路径）
  
     ```bash  
     rm -rf /usr/bin/nvc
     ```
  
  7. 去查看进程 
  
     ```bash
     nvidia-smi
     ```
  
  8. 删除进程
  
     ```bash
     kill -9 [进程号]
     ```
  
- 服务器安全升级

	1.禁用root用户登录
	首先通过修改ssh配置文件禁用root用户远程登录
	
	```bash
	nano /etc/ssh/sshd_config
	```
	将配置文件中的：
	```bash
	PermitRootLogin yes
	```
	改成:
	```bash
	PermitRootLogin no
	```
	然后重启ssh使改变生效：
	```bash
	sudo systemctlrestart ssh.service
	````
